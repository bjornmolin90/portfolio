{% include 'incl/header.twig' %}

<body>
    {% include 'incl/nav.twig' %}

    <div class="main landingpage" role="main">
        {{ content }}

        {% for page in pages("technology") %}
            <a href="{{ page.url }}">
            <div class="tech-box {{ page.title }}">
                <h2> {{ page.title }} </h2>
            </div>
            </a>
        {% endfor %}
    </div>

    {% include 'incl/footer.twig' %}
</body>

</html>
